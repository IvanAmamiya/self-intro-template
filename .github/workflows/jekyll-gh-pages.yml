name: Deprecated Jekyll Pages Workflow (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Jekyll workflow disabled
        run: echo "Disabled. Use .github/workflows/deploy-pages.yml for Vite deploy."
